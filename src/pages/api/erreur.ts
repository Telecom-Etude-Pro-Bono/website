import { getToken } from "./helloasso";

export const prerender = false

// export const GET = async ({ request }) => {
//     return new Response(JSON.stringify({ error: 'Method not allowed' }), { status: 405 });
// }
